# 参与贡献

感谢你愿意改进 Digital Studio。这个仓库欢迎小而明确、能够被验证的贡献，尤其是：

- 可访问性、键盘操作和减少动效支持。
- 响应式布局、浏览器兼容性和性能问题。
- 内容模型、类型安全和组件边界的改进。
- 文档错误、失效链接和可复现的视觉缺陷。

请不要直接改写作者的职业事实、个人介绍或公开指标，也不要用生成内容补齐缺失的真实项目证据。相关疑问请先开 Issue。

## 本地开发

```bash
pnpm install
pnpm dev
```

提交 Pull Request 前运行：

```bash
pnpm run typecheck
pnpm run build
```

涉及视觉改动时，同时检查 1440 × 1000 与 390 × 844，确认没有横向溢出，交互可通过键盘访问，并遵守 `prefers-reduced-motion`。

## 提交建议

1. 较大功能、视觉方向变化或新增依赖，请先开 Issue 说明问题、方案与边界。
2. 每个 Pull Request 聚焦一个主题，避免夹带无关格式化或重构。
3. 描述改动动机、验证方式和受影响页面；视觉变化请附前后截图。
4. 如果使用 AI 生成代码或素材，请说明使用范围，并由提交者承担事实、授权和质量检查责任。
5. 不要提交真实凭证、后台 URL、本机路径、私人联系方式或缺少授权的素材。

提交贡献即表示你有权提供相关代码或素材，并同意软件代码按本仓库的 MIT License 分发。个人内容与素材仍受 [`CONTENT-NOTICE.md`](./CONTENT-NOTICE.md) 约束。

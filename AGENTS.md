# Personal Hub 项目说明

## 项目目的

本仓库维护个人网站 **Zhiyong Chen · Digital Studio**。网站应帮助招聘者、合作伙伴和同行快速理解：

1. Zhiyong 是一位经验丰富的 B2B SaaS / 数据产品负责人。
2. 他能够把产品判断转化为原型、AI 工作流和可运行代码。
3. 他持续在写作、小说、音乐、视频和视觉实验等领域进行创作。

## 设计方向

- 视觉概念：编辑工作室 × 数字控制室。
- 核心色板：墨黑、暖纸色、信号蓝、酸性绿。
- 保留充裕的编辑式留白、不对称项目布局、有力量的排版和克制的动效。
- 避免通用卡片仪表盘、紫色渐变、图库素材和缺少叙事目的的装饰效果。
- 响应式行为必须经过有意识的设计。移动端应重新编排布局，而不是简单缩放桌面页面。

## 内容规则

- 结构化网站文案位于 `src/data/siteContent.ts`。
- 不要公开简历中的电话号码。
- 公开联系邮箱为 `chenzy94@sina.com`。
- 不得虚构当前雇主信息、奖项、营收、用户数量或公开账号指标。
- 只有同一页面中的项目证据能够支撑时，才允许使用“全周期构建者”等愿景式定位。
- 指标变化时，同时更新 `src/data/siteContent.ts` 和 `docs/content-sources.md`。
- 在确认公开视频主页 URL 和代表作品之前，视频频道保持占位状态。

## 资产

- 网站自有资产放在 `public/` 下。
- 优先使用相邻仓库中的真实项目资产，不要使用通用图库素材或生成式填充内容。
- 在视觉质量仍然可接受时，将复制进来的大型 PNG 转换为 WebP。
- 不要修改相邻项目中的源资产；先复制到本仓库。
- 所有内容图片都应保留有意义的 `alt` 文本。

## 验证

运行最窄的相关检查：

```powershell
pnpm run typecheck
pnpm run build
```

涉及视觉改动时，至少验证：

- 桌面端：1440 × 1000
- 移动端：390 × 844
- 没有横向溢出
- 菜单、能力标签页、外部链接和邮件链接仍可通过键盘访问
- 遵守 `prefers-reduced-motion`

## 部署

- 用户已授权使用 Cloudflare Pages 免费托管主页；只上传构建后的静态文件，不连接 GitHub、不申请仓库权限、不配置自动部署。更新方法见 `docs/deployment.md`。
- 发布网站与维护 Git 仓库分别处理；用户请求静态发布不意味着授权额外的仓库推送或云端代码维护。
- 不添加分析、表单、追踪、数据库或付费服务，除非用户另行明确要求。
